try {
  // Код, который может вызвать исключение
  // Но: такая конструкция недопустима
  // try должен иметь хотя бы один блок catch или блок finally
}
// Ошибка компиляции: 'try' without 'catch', 'finally' or resource declarations